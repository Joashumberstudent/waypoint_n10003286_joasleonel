# Waypoint

Waypoint is a Django web application built around a reusable
Python domain engine for trails, distances, and itineraries.

## Requirements

- Python 3.12
- Django 4.2

## Setup

Clone the repository:

```bash
git clone <repository-url>
cd Waypoint_week9